plugins {
    id 'java'
}

subprojects {
    group = 'io.github.demo'
    version = '1.0.0'
    sourceCompatibility = '1.8'

    repositories {
        mavenCentral()
    }

    ext {
        set('springCloudVersion', "Greenwich.SR1")
    }

}
